{
  "scripts": {
    "build": "browserify -p esmify src/app.js -o dist/app.js",
    "lint": "standard --fix *.js src/*.js",
    "watch": "watchify -p esmify --debug src/app.js -o dist/app.js -v"
  },
  "dependencies": {
    "chart.js": "^4.3.0",
    "csvtojson": "^2.0.10"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "esmify": "^2.1.1",
    "standard": "^17.0.0",
    "watchify": "^4.0.0"
  }
}
